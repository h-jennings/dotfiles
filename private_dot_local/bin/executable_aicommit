#!/bin/bash

if ! git rev-parse --git-dir >/dev/null 2>&1; then
  echo "aicommit: not a git repository" >&2
  exit 1
fi

if git diff --staged --quiet; then
  echo "aicommit: nothing staged" >&2
  exit 1
fi

git diff --staged | pbcopy
open "raycast://ai-command/git-commit-message"
